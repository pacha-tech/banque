# Utiliser les variables d'environnement, sinon utiliser les valeurs par défaut
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/tp_304_group}
spring.datasource.username=${DB_USER:dev_user}
spring.datasource.password=${DB_PASSWORD:password123}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.MySQL8Dialect}
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}